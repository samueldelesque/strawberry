(function(){
	'use strict';

	describe('Strawberry.login module', function() {

		beforeEach(module('Strawberry.login'));

		describe('login controller', function(){

			it('should ....', inject(function($controller) {
				//spec body
				var login = $controller('loginCtrl');
				expect(SignupCtrl).toBeDefined();
			}));
	  });
	});
})()